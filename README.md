Interfaz de línea de comandos para crear un badge
=================================================

**Instalación** 
```
$ pipenv install
$ pipenv shell
```

**Uso**
```
$ badge create [fichero.json] [fichero.png]
$ badge --help
```
